<h2>
 <%= link_to @project.title, project_path(project) %>
</h2>
<h3>
 <%= image_tag @project.image.url(:thumb) %>
</h3>
<p>
 <%= @project.description %>
</p>
<%= link_to "Create a Team", new_project_team_path %>
